import 'package:connectinno_case_mobile/core/router/routes.dart';
import 'package:go_router/go_router.dart';

/// Centralized router for the application using `GoRouter`.
final appRouter = GoRouter(
  routes: $appRoutes,
);
